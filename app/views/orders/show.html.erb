<p id="notice"><%= notice %></p>

<p>
  <strong>Order date:</strong>
  <%= @order.order_date %>
</p>

<p>
  <strong>User:</strong>
  <%= @order.uemail %>
</p>

<p>
  <strong>Status:</strong>
  <%= @order.status %>
</p>

<%= link_to 'Edit', edit_order_path(@order) %> |
<%= link_to 'Back', orders_path %>



 <% @orderitem.each do |item| %>
 <h1><%= item.name %>, <%= item.size %>, <%= item.quantity %></h1>
 
 
 <% end %>
